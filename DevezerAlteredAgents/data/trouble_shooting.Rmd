---
title: "Untitled"
author: "Victor Møller"
date: "November 24, 2019"
output: html_document
---

```{r setup, include=FALSE}

library(tidyverse)

setwd("C:/Users/victm/Documents/CRUST/data")
TESS <- read_csv2("Tess.csv")
BO <- read_csv2("Bo.csv")

#TESS
TESS <- TESS %>%
  filter(strategy == 2)

#BO 
BO <- BO %>%
  filter(strategy == 3)

#MORE TESS
TESS_ISSUE <- TESS %>%
  filter(selected_model == initial_global_model) %>%
  group_by(selected_model) %>%
  summarise(count = n())

#MORE BO
BO_ISSUE <- BO %>%
  filter(selected_model == initial_global_model) %>%
  group_by(selected_model) %>%
  summarise(count = n())

## BO & TESS SOFT ##
SOFT <- read_csv2("output.csv")

BO_SOFT <- SOFT %>%
  filter(strategy == 3)

TESS_SOFT <- SOFT %>%
  filter(strategy == 2)

#MORE TESS
TESS_ISSUE <- TESS_SOFT %>%
  filter(selected_model == initial_global_model) %>%
  group_by(selected_model) %>%
  summarise(count = n())

#MORE TESS
BO_ISSUE <- BO_SOFT %>%
  filter(selected_model == initial_global_model) %>%
  group_by(selected_model) %>%
  summarise(count = n())

```

